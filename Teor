/*Стандартна бібліотека шаблонів (STL) - це частина Стандартної бібліотеки
С++, яка містить набір шаблонів контейнерних класів (наприклад, std :: vector і std :: array),
алгоритмів і ітераторів.
std :: vector - це контейнер послідовності, який інкапсулює масиви динамічного розміру.
Елементи зберігаються безперервно, а це означає, що до них можна отримати доступ не тільки за допомогою ітераторів,
але й за допомогою зрушень. Це означає, що вказівник на елемент вектора може бути переданий на будь-яку функцію,
яка очікує вказівника на елемент масиву.
std :: array - це контейнер, який інкапсулює масиви фіксованого розміру.
Структура поєднує продуктивність та доступність масиву у стилі С із перевагами стандартного контейнера,
наприклад, знанням його власного розміру, підтримкою призначення, ітераторами випадкового доступу тощо*/
#include <iostream>
#include <string>
#include <vector>
#include <cmath>


int main()
{
    std::vector<double> vec;
    double current = 0.0,
           norm    = 0.0,
           sum     = 0.0;
    int dimension = 0;

    std::cout << "Enter dimension of vector: ";
    std::cin >> dimension;

    vec.reserve(dimension); //резервуємо пам'ять

    for ( int i = 0; i < dimension ; i++ ){
        std::cout << "Enter "<< i <<"-th coordinate of vector: ";
        std::cin >> current ;
        vec.push_back(current); //додаємо елемент до вектора
    }
    for ( int i = 0; i < dimension ; i++ ){
        sum += vec[i]*vec[i];
    }
    norm = std::sqrt(sum);
    std::cout << "The norm of the vector is " << norm << std::endl;
}
